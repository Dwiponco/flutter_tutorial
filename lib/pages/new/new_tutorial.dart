import 'package:flutter/material.dart';

class NewTutorial extends StatelessWidget {
  const NewTutorial({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("cobain nih"),
      ),
    );
  }
}